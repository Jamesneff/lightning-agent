# LLM
openai>=1.0.0

# Notion API
notion-client>=2.2.1

# HTTP / scraping
httpx>=0.27.0
beautifulsoup4>=4.12.0
feedparser>=6.0.11

# Data handling
python-dotenv>=1.0.0
pydantic>=2.7.0

# Deduplication utilities
thefuzz>=0.22.1          # fuzzy string matching for near-duplicate detection
python-Levenshtein>=0.25.0  # speeds up thefuzz

# Persistence (deduplicator SQLite backend is stdlib; Redis is optional)
# redis>=5.0.0           # uncomment if using DEDUP_BACKEND=redis
